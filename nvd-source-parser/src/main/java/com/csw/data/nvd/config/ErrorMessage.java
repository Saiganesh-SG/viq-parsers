package com.csw.data.nvd.config;

public class ErrorMessage {
    public static final String INVALID_PARSE_TYPE = "Invalid parse type. The parse type should be any of the one (CVE, CPE, CWE and Exploit)";
}
